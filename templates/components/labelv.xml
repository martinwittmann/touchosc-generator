<control
  type="labelv"
  name="{{ name }}"
  x="{{ x }}"
  y="{{ y }}"
  w="{{ w }}"
  h="{{ h }}"
  color="{{ color }}"
  text="{{ text }}"
  size="{{ size }}"
  background="{{ background }}"
  outline="{{ outline }}"
></control>